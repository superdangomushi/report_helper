# レポート作成ガイド

このフォルダのファイルを編集してから、次のコマンドでレポート（.docx）を生成します。

```sh
mkreport <このフォルダのパス>
# 例:
mkreport .                    # カレントディレクトリから生成
mkreport ./my_first_report    # 別フォルダから生成
```

生成された `.docx` はプロジェクトフォルダ内に `<プロジェクト名>.docx` として保存されます。出力先を変えたい場合は第2引数で指定します。

```sh
mkreport ./my_first_report ~/Desktop/レポート.docx
```

---

## ファイル構成

```
project/
├── README.md          ← このファイル
├── seed.docx          ← レポートのテンプレート（書式・タイトルページ）
├── hyo1.docx          ← 表のスタイル参照
├── title.env          ← タイトルページのプレースホルダー値
├── 1mokuteki.md       ← 1. 目的
├── 2souchi.md         ← 2. 実験装置
├── 3houhou.md         ← 3. 実験方法
├── 4kekka.md          ← 4. 実験結果
├── 5kousatsu.md       ← 5. 考察
├── 6inyou.env         ← 6. 引用（CSV: 年,著者,タイトル）
├── figures/           ← 図のPNG/JPEG画像を入れる
├── ocr_hyo/           ← OCRさせたい表の画像を入れる
└── .env               ← (任意) GEMINI_API_KEY=... など
```

---

## title.env の書き方

`#1`〜`#17`、`#T1` は seed.docx のタイトルページ上の位置に対応します。

```
#1=創大くんの生態
#2=2026/05/15
#3=1X24B044
#4=北川創大,A
#5=63号館地下1階
#6=22.5
#7=45
#8=10:00
#9=15:30
#10=共同実験者氏名
#11=学籍番号
...
#T1=ここに摘要を書く。
```

| キー | 位置 |
|------|------|
| #1 | 実験項目 |
| #2 | 実施日 |
| #3 | 学籍番号 |
| #4 | 氏名,班番号（カンマ区切り。例: `#4=北川創大,A`） |
| #5 | 実験場所 |
| #6 | 室温 |
| #7 | 湿度 |
| #8 | 開始時刻 |
| #9 | 終了時刻 |
| #10〜#17 | 共同実験者 |
| #T1 | 摘要 |

---

## 本文（.md ファイル）の記法

### 句読点の自動変換

レポート規約に合わせて、本文・表・タイトル・引用に含まれる全角の `、` と `。` は、生成時に自動で半角の `,` と `.` に変換されます。元の `.md` / `title.env` / `6inyou.env` は書き換えずそのままで構いません（数式や画像ファイル名は変換対象外）。

### 段落

普通にテキストを書けば段落になります。空行で段落が分かれます。

### 表（Markdown）

```
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| a   | b   | c   |
| d   | e   | f   |
```

`hyo1.docx` の罫線スタイル（上ダブル線・左右なし）で出力されます。

#### セルの結合（横・縦）

Word の表のように、セルを結合できます。マーカーは出力時に自動で取り除かれます。

**横方向の結合**: 結合範囲の左端を `<-`、右端を `->` で挟みます。結合後のセルは中央揃え。
揃え方は `/c`（中央・既定）・`/l`（左）・`/r`（右）で指定できます。

```
| 試料 | 測定値 [W/m·K] | <-/c理論値 [W/m·K] | c/-> | 誤差 [%] |
|------|----------------|----------------|--------|----------|
| 銅 | 395 | 401 | 1.5 | 10 |
```

→ 「理論値 [W/m·K]」が 2 列分に結合され中央揃えになります。

**縦方向の結合**: 同じ列で、結合範囲を `-^` で挟みます。最初の `-^` が開始（このセルの文字が残る）、
次の `-^` が終了で、あいだの行は空にします。

```
| 試料 | 測定値 [W/m·K] | 理論値 [W/m·K] | 誤差 [%] |
|------|----------------|----------------|----------|
| 銅 | 395-^ | 401 | 1.5 |
| アルミニウム |  | 237 | 3.0 |
| アルミニウム | -^ | 237 | 3.0 |
```

→ 「測定値」列の 3 行が「395」で 1 つに結合されます。`-^` のペアごとに 1 つの結合になります。

### 図の挿入

```
` ` ` 1-1-1ファイル名.png ` ` `
```

（実際は半角バッククォート3つで囲ってください）

`figures/` に置いた画像を挿入し、その下に中央揃えで `図1.1.1 ファイル名` のキャプションを自動で付けます。

**ファイル名の命名規則**: `N-N-N<名前>.<拡張子>`
- `1-1-1温度分布.png` → 「図1.1.1 温度分布」
- `2-3-1グラフ.jpg`   → 「図2.3.1 グラフ」

### OCR 表（Gemini）

```
` ` ` 1-1-1表名.png ` ` `
```

`ocr_hyo/` に置いた表の画像が Gemini API で OCR され、Markdown 表として認識されてから docx の表として埋め込まれます。
画像上に中央揃えで `表N.N.N <名前>` のキャプションを付けます。

**`--ocr` フラグでのみ実行**: API 呼び出しを伴うので、明示的に `--ocr` を付けたときだけ OCR が走ります。

```
mkreport --ocr ./my_first_report          # OCR 実行
mkreport ./my_first_report                # OCR スキップ（キャプションだけプレースホルダ表示）
```

**OCR 結果は .md に書き戻される**: OCR が成功すると、元の `.md` の `` ```ファイル名.png``` `` のフェンスが下記のブロックに置き換えられます。

```
<OCR-2-1-1計測値表.png>
/c 表2.1.1 計測値表 c/

| 試料 | 値1 | 値2 |
| --- | --- | --- |
| A | 100 | 200 |
</OCR-2-1-1計測値表.png>
```

- これ以降、`--ocr` なしで build しても表は描画されます（キャッシュ）。
- 中身を手で編集すれば編集後の表が出力されます。
- `--ocr` をもう一度付けて build すると、ブロック内が新しい OCR 結果で上書きされます（やり直したいとき用）。

**APIキー設定**: プロジェクト直下の `.env` か環境変数で:

```
GEMINI_API_KEY=your_api_key_here
```

**失敗時の挙動**: APIキー未設定・無効・通信失敗・解析失敗のいずれも、エラーで止まらずに表の位置に「失敗」とだけ書き込みます。

### 数式

````
```
E = mc^2
```
````

連番で `(1), (2), ...` の式番号が右端に振られます（セクションをまたいで通し番号）。

**数式記法（LaTeX 風）**:

| 記法 | 意味 | 例 |
|------|------|-----|
| `x^2` | 上付き | x の2乗 |
| `x^{2y}` | 上付き（複数文字は `{}` で括る） | x の2y乗 |
| `x_1` | 下付き | x_1 |
| `T_corr` | 下付き（連続する英字は `{}` 不要） | T_corr |
| `x_{ij}` | 下付き（複数文字） | x_{ij} |
| `\frac{a}{b}` | 分数 | a/b（バー付き） |
| `\sqrt{x}` | 平方根 | √x |
| `\sqrt[n]{x}` | n乗根 | ⁿ√x |
| `*` | 積 | × |

（文字の上に付けるアクセント、ギリシャ文字、各種記号は下の一覧を参照）

#### アクセント記号（文字の上に付ける）

| 記法 | 表示 | 意味 |
|------|------|------|
| `\bar{x}` / `\overline{x}` | x̄ | バー（平均値などに） |
| `\hat{x}` / `\widehat{x}` | x̂ | ハット |
| `\tilde{x}` / `\widetilde{x}` | x̃ | チルダ |
| `\vec{v}` / `\overrightarrow{v}` | v⃗ | ベクトル |
| `\dot{x}` | ẋ | 1階微分（時間微分） |
| `\ddot{x}` | ẍ | 2階微分 |
| `\acute{x}` | x́ | アキュート |
| `\grave{x}` | x̀ | グレイブ |
| `\check{x}` | x̌ | チェック（キャロン） |
| `\breve{x}` | x̆ | ブレーブ |
| `\mathring{x}` | x̊ | リング |

#### ギリシャ文字

| 記法 | 表示 | 記法 | 表示 | 記法 | 表示 |
|------|------|------|------|------|------|
| `\alpha` | α | `\beta` | β | `\gamma` | γ |
| `\delta` | δ | `\epsilon` | ε | `\varepsilon` | ϵ |
| `\zeta` | ζ | `\eta` | η | `\theta` | θ |
| `\vartheta` | ϑ | `\iota` | ι | `\kappa` | κ |
| `\lambda` | λ | `\mu` | μ | `\nu` | ν |
| `\xi` | ξ | `\omicron` | ο | `\pi` | π |
| `\varpi` | ϖ | `\rho` | ρ | `\varrho` | ϱ |
| `\sigma` | σ | `\varsigma` | ς | `\tau` | τ |
| `\upsilon` | υ | `\phi` | φ | `\varphi` | ϕ |
| `\chi` | χ | `\psi` | ψ | `\omega` | ω |

大文字版（先頭を大文字にする）: `\Gamma` Γ, `\Delta` Δ, `\Theta` Θ, `\Lambda` Λ, `\Xi` Ξ, `\Pi` Π, `\Sigma` Σ, `\Upsilon` Υ, `\Phi` Φ, `\Psi` Ψ, `\Omega` Ω

#### 演算記号・関係子

| 記法 | 表示 | 意味 |
|------|------|------|
| `\times` | × | 乗算（かけ算）。`*` でも × になります |
| `\cdot` | · | 中点（積を表す点） |
| `\div` | ÷ | 除算（わり算） |
| `\pm` | ± | プラスマイナス（誤差表記など） |
| `\mp` | ∓ | マイナスプラス |
| `\le` / `\leq` | ≤ | 以下 |
| `\ge` / `\geq` | ≥ | 以上 |
| `\ne` / `\neq` | ≠ | 等しくない |
| `\approx` | ≈ | ほぼ等しい（近似） |
| `\simeq` | ≃ | 漸近的に等しい |
| `\cong` | ≅ | 合同 |
| `\equiv` | ≡ | 恒等・合同（定義） |
| `\sim` | ∼ | 相似・〜程度 |
| `\propto` | ∝ | 比例する |
| `\ll` | ≪ | 非常に小さい |
| `\gg` | ≫ | 非常に大きい |

#### 矢印

| 記法 | 表示 | 意味 |
|------|------|------|
| `\to` / `\rightarrow` | → | 右向き矢印（〜になる、極限など） |
| `\leftarrow` | ← | 左向き矢印 |
| `\leftrightarrow` | ↔ | 左右両向き |
| `\Rightarrow` | ⇒ | ならば（含意） |
| `\Leftarrow` | ⇐ | 逆向きの含意 |
| `\Leftrightarrow` | ⇔ | 同値（必要十分条件） |
| `\mapsto` | ↦ | 写像（〜を〜へ移す） |

#### 大型演算子

| 記法 | 表示 | 意味 |
|------|------|------|
| `\sum` | ∑ | 総和。範囲は `\sum_{i=1}^{N}` のように上下付きで |
| `\prod` | ∏ | 総乗 |
| `\int` | ∫ | 積分 |
| `\oint` | ∮ | 周回積分 |
| `\iint` | ∬ | 二重積分 |
| `\iiint` | ∭ | 三重積分 |
| `\lim` | lim | 極限。`\lim_{x \to 0}` のように |

#### 集合・論理

| 記法 | 表示 | 意味 |
|------|------|------|
| `\in` | ∈ | 〜に属する |
| `\notin` | ∉ | 〜に属さない |
| `\subset` | ⊂ | 部分集合 |
| `\supset` | ⊃ | 上位集合 |
| `\subseteq` | ⊆ | 部分集合（等号を含む） |
| `\supseteq` | ⊇ | 上位集合（等号を含む） |
| `\cup` | ∪ | 和集合 |
| `\cap` | ∩ | 積集合（共通部分） |
| `\emptyset` | ∅ | 空集合 |
| `\forall` | ∀ | すべての（全称） |
| `\exists` | ∃ | ある〜が存在する（存在） |
| `\land` | ∧ | かつ（論理積） |
| `\lor` | ∨ | または（論理和） |
| `\lnot` | ¬ | 〜でない（否定） |

#### その他の記号

| 記法 | 表示 | 意味 |
|------|------|------|
| `\infty` | ∞ | 無限大 |
| `\partial` | ∂ | 偏微分 |
| `\nabla` | ∇ | ナブラ（勾配・発散・回転） |
| `\hbar` | ℏ | ディラック定数（換算プランク定数） |
| `\ell` | ℓ | 筆記体の l（長さなど） |
| `\prime` | ′ | プライム（微分のダッシュ） |
| `\dprime` | ″ | ダブルプライム |
| `\cdots` | ⋯ | 中央そろえの3点（a + ⋯ + z） |
| `\ldots` | … | 下そろえの3点（a, …, z） |
| `\vdots` | ⋮ | 縦の3点 |
| `\ddots` | ⋱ | 斜めの3点（行列の対角） |

#### 関数名（直立体で表示）

`\sin` sin, `\cos` cos, `\tan` tan, `\cot` cot, `\sec` sec, `\csc` csc,
`\arcsin` arcsin, `\arccos` arccos, `\arctan` arctan,
`\sinh` sinh, `\cosh` cosh, `\tanh` tanh,
`\log` log, `\ln` ln, `\exp` exp, `\max` max, `\min` min

> これらはイタリック体（変数）ではなく直立体で表示されます。例: `\sin(\theta)` → sin(θ)

**例**:

````
```
\sigma = \sqrt{\frac{1}{N} \sum (x_i - \bar{x})^2}
```
````

### 整列マーカー

C++のブロックコメント風に `/c ... c/` と `/r ... r/` で囲った範囲を中央／右揃えにできます。マーカーが無い箇所は左揃え（既定）です。複数段落をまたいでも有効です。

```
/c
この段落は中央揃え。
複数行もOK。
c/

何もない段落は左揃え。

/r この一文は右揃え。 r/
```

---

## 6inyou.env（引用）

1行 = 1件の引用。CSV 形式で `年,著者,タイトル` の順に書きます。

```
2026,速稲田大学理工学術院,あいうえお
2025,北川,書きくけこ
```

Word では次のように番号付きで出力されます:

```
[1] 速稲田大学理工学術院, あいうえお, 2026.
[2] 北川, 書きくけこ, 2025.
```

---

## コマンド一覧

| コマンド | 説明 |
|----------|------|
| `mkreport start <プロジェクト名>` | テンプレートからプロジェクトを新規作成 |
| `mkreport <プロジェクトパス>` | プロジェクトからレポートを生成 |
| `mkreport <プロジェクトパス> <出力>` | 出力先を明示してレポートを生成 |
| `mkreport help` | 使い方を表示 |
