熱伝導率はフーリエの法則より、フーリエの法則の微分形は次のように書ける。

```
q = -k \frac{dT}{dx}
```

試料の断面積をAとすると、定常状態における熱流量Qは次式で表される。

```
Q = k * A * \frac{T_1 - T_2}{L}
```

やっほー

```
Nu = \frac{h * L}{k}
```

また平均誤差は次の標準偏差で評価する。

```
\sigma = \sqrt{\frac{1}{N} \sum (x_i - \bar{x})^2}
```
